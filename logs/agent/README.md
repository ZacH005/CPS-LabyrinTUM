# Agent Logs

AI and human contributors can add short handoff logs here using:

```text
docs/AI_AGENT_LOG_TEMPLATE.md
```

Log files are ignored by Git by default to avoid noisy local history, but this README is tracked.

